//! Track B1 probe — see `tests/global_survives_gc.rs`. The crate itself is empty;
//! the proof lives in the integration test, which drives Nova's public embedder
//! API (Agent / Realm / parse_script / script_evaluation / Global).
