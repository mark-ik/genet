# Namespace claims

The following crates.io names were claimed by this project on 2026-07-13:

- `cambium`
- `meristem`
- `sprigging`
- `genet`
- `genet-stylo`

The first three belong to this workspace. `genet` names the adjacent browser
engine currently housed in the `serval` repository, and `genet-stylo` names its
Stylo fork package. A claimed name establishes namespace ownership;
it does not by itself mark an implementation release as ready.
