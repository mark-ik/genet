//! Name claim for the `serval-scripted-dom` component of mark-ik/serval.
//!
//! The real crate lives (unpublished) in that repository; consume it as a
//! git dependency. This claim exists so dependents that name it with a
//! version requirement can publish; it carries no API.
