[package]
name = "serval-taffy"
version = "0.0.1"
edition = "2021"
license = "MIT"
repository = "https://github.com/mark-ik/serval"
description = "Serval's taffy fork (vendored 0.12.x line with serval-side patches). NAME CLAIM: the real crate lives in mark-ik/serval (support/patches/taffy); consume it as a git dependency."

[workspace]
