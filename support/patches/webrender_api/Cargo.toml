[package]
name = "webrender_api"
version = "0.68.0"
edition = "2021"
publish = false

# Stub replacing the webrender_api/ crate that lived in the
# webrender-wgpu workspace (renamed to netrender, dropped
# webrender_api as a separate crate).
#
# Restores workspace loadability for servo-wgpu while consumers
# get migrated to netrender's surface (sub-phase 5.5 work).
# Anything importing webrender_api types will fail to compile
# against this stub by design — those imports become the
# migration targets.
