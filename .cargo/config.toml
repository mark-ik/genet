# Local path overrides (gitignored; local-dev only) — mirrors
# repos/mere/.cargo/config.toml. serval builds its renderer + script-engine
# git-deps against the in-sync local checkouts, so a cross-repo version skew
# can never block the build. Delete this file to fall back to the published
# git-deps (a clean clone builds without it).
#
# serval needs no *committed* build settings: the inherited servo target
# linkers (Android / UWP / Windows-non-x64) and the RUSTC_BOOTSTRAP whitelist
# (crown / script / mozjs / style_tests, all workspace-excluded) were verified
# unused, and the e2e builds + passes with the default linker and stack.
paths = [
    "C:/Users/mark_/Code/repos/netrender",
    "C:/Users/mark_/Code/crates/nova",
    "C:/Users/mark_/Code/crates/boa",
    "C:/Users/mark_/Code/crates/piccolo",
]
